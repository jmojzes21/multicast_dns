name: multicast_dns
description: Dart package for performing mDNS queries (e.g. Bonjour, Avahi).
publish_to: none
version: 0.3.2+7

environment:
  sdk: ^3.3.0

dependencies:
  meta: ^1.3.0

dev_dependencies:
  test: "^1.16.5"

topics:
  - bonjour
  - mdns
  - network
